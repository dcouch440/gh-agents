//! Documenter executor — phased pipeline for document generation.
//!
//! Strategy → Research → Write pipeline. The executor and DAG integration
//! will be added in a subsequent chunk; this module currently provides
//! only the shared types.

pub mod types;
