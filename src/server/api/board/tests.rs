#[cfg(test)]
mod tests {
    // Handler tests require a running Axum test server with AppState.
    // Service-level tests in services/board/tests.rs cover the business logic.
    // Integration tests for the HTTP layer can be added when a test harness exists.
}
