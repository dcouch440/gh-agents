We need to design a react context architecure that is 1 to 1 with our database schema. That way we can match the web socket events in the UI in the same order.

We want events to be smooth and for it to be reactive. Shared state where required and setup well. Production ready code.