# Context injection tools

An agent uses a tool similar to explore and the reports are saved.
During the agents next executions file paths are combined with relevant chunks injected into system prompt as context using RAG.