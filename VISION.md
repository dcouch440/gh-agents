# Context injection tools

So heres my next question. Most likely next, we are going to create another system agent but instead its an agent to create the nodes and
topology for the next layer down. Idk what to call it or if the systems setup for it but I was thinking maybe you talk to the manager  
 and the manager does it right from the conversation, making updates to the nodes and sending them to the front end as a draft. THe thing  
 is this is complicated but not really its just that with every message the user sends we send a board snapshot and convert that to a  
 "repo" the agent is currently "living in". Much like our system agents the node agent Can make updates, or they can dispatch to a real  
 builder.
