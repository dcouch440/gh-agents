# issues
- Make complete_system throw errors if required files and fields are missing.
- If an LLM ends without calling complete_system. Throw an error and wake the LLM back up.
- Research the workflow agent and see if there are similar issues. If so, fix them as well. We should make the system re-usable based on dependencies required in their "repo".