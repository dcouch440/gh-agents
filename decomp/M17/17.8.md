# Ticket 17.8: Cleanup & Documentation

> Remove all SQLite remnants, build optional data migration tool, update documentation.

## Goal

Zero SQLite references in the codebase. Documentation reflects Postgres setup. Optional migration tool exists for users with existing SQLite data.

**Checkpoint**: `grep -r "sqlite\|SqlitePool\|state\.db" src/` returns zero results. Docs are updated.

---

## Context

After all queries and consumers are migrated, there will be dead SQLite code, unused dependencies, and outdated documentation. This ticket cleans everything up.

**Key files**:
- `Cargo.toml` — Remove sqlite dependencies
- `src/` — Remove any remaining SQLite references
- `docs/` — Update setup and configuration guides
- `docker-compose.yml` — Verify Postgres is the only database service

**Dependencies**: 17.7 (all tests must pass first)

---

## Slices

### Slice 17.8.1: Remove SQLite Dependencies

**Do this**:
- Remove `sqlx` sqlite feature from Cargo.toml
- Remove `rusqlite` if present
- Remove `tempfile` from dev-dependencies if no longer used
- Remove any SQLite-specific utility code
- Run `cargo check` to verify nothing breaks

**Verify**:
- [ ] No SQLite crate in `Cargo.lock`
- [ ] `cargo check` passes
- [ ] `cargo clippy` clean

---

### Slice 17.8.2: Code Cleanup

**Do this**:
- Search for and remove any remaining SQLite references in source code
- Remove `.nexor/state.db` path handling
- Remove any SQLite-specific error handling or fallback code
- Remove the custom migration runner file if not already deleted
- Clean up any `#[cfg]` flags related to SQLite

**Verify**:
- [ ] `grep -ri "sqlite" src/` returns zero results
- [ ] `grep -ri "state\.db" src/` returns zero results
- [ ] No dead code warnings

---

### Slice 17.8.3: Data Migration Tool

**Do this**:
- Create `src/bin/migrate_sqlite_to_pg.rs` (optional binary)
- Reads from `.nexor/state.db` (SQLite)
- Connects to `DATABASE_URL` (Postgres)
- Copies all rows from each table, converting types
- Prints progress and row counts
- Add to Cargo.toml as `[[bin]]`

**Verify**:
- [ ] `cargo build --bin migrate_sqlite_to_pg` compiles
- [ ] Tool migrates data from a test SQLite database to Postgres
- [ ] Row counts match after migration

---

### Slice 17.8.4: Documentation Updates

**Do this**:
- Update `docs/installation.md` — Postgres is now required
- Update `docs/configuration.md` — `DATABASE_URL` env var
- Update `docs/docker.md` — Docker Compose includes Postgres
- Update `README.md` if it mentions SQLite
- Update `CLAUDE.md` if it references SQLite or `.nexor/state.db`

**Verify**:
- [ ] All docs reference Postgres, not SQLite
- [ ] Setup instructions work for a new developer
- [ ] `PROGRESS.md` updated with M17 completion

---

## Completion Checklist

Before marking this ticket done:

- [ ] All slices verified
- [ ] Zero SQLite references in codebase
- [ ] Migration tool compiles and works
- [ ] All documentation updated
- [ ] `cargo check` && `cargo test` && `cargo clippy` all pass
- [ ] `PROGRESS.md` updated
