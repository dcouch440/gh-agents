# Ticket 20.7: Integration Tests

> End-to-end tests verifying the full feed flow from WS message to rendered output.

## Goal

Integration tests confirm that WS messages flow through the store to rendered components correctly.

**Checkpoint**: All integration tests pass, coverage meets standards.

---

## Context

These tests mock the WebSocket layer and verify the full pipeline: message → store → hook → component render.

**Key files**:
- `cli/src/components/MainView.test.tsx` — Extend with integration scenarios
- `cli/src/api/ws.test.ts` — Extend if needed

**Dependencies**: 20.6

---

## Slices

### Slice 20.7.1: Feed Flow Integration

**Do this**:
- Test: mock WS emits a `feed` message → FeedArea renders the item
- Test: mock WS emits a `task_update` → task state updated in store
- Test: mock WS emits an `agent_update` → agent state updated in store
- Test: multiple rapid messages render in order

**Verify**:
- [ ] Tests pass

---

### Slice 20.7.2: Command Integration

**Do this**:
- Test: type `/help` in InputBar → system message appears in feed
- Test: type `/clear` → feed items cleared
- Test: type `/unknown` → error message appears in feed
- Test: type regular message → api.chat.send called

**Verify**:
- [ ] Tests pass

---

### Slice 20.7.3: Connection State Integration

**Do this**:
- Test: on WS connect → StatusBar shows green dot + "Connected to server" in feed
- Test: on WS disconnect → StatusBar shows red dot + "Disconnected" in feed
- Test: on WS reconnect → StatusBar updates back to green

**Verify**:
- [ ] Tests pass
- [ ] `cd cli && npm test` all green
- [ ] `PROGRESS.md` updated

---

## Completion Checklist

- [ ] All slices verified
- [ ] Full feed pipeline tested
- [ ] Command pipeline tested
- [ ] Connection state tested
- [ ] `PROGRESS.md` updated
