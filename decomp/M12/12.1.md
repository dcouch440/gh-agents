# Ticket 12.1: Chat View

> Orchestrator conversation interface with streaming responses.

## Goal

Chat interface for talking with the AI orchestrator, with real-time streaming responses.

**Checkpoint**: Can send messages, see streaming responses, markdown rendered correctly.

---

## Context

This is the primary interaction point - where users chat with the orchestrator.

**Key files**:
- `ui/src/hooks/useChat.ts` - Chat data hook from M11
- `ui/src/api/client.ts` - API client

**Dependencies**:
- M11 (React foundation)
- M10.3 (Chat API endpoints)

---

## Slices

### Slice 12.1.1: Create ChatPage with Message List

**Do this**:
- Create ChatPage component
- Display message history
- Scroll to bottom on new messages
- Use CSS modules for styling
- Add proper cleanup for useEffect

**Create `ui/src/pages/ChatPage/ChatPage.tsx`**:
```tsx
import { useEffect, useRef } from 'react';
import { useChat } from '../../hooks/useChat';
import { Message } from '../../components/Message';
import { ChatInput } from '../../components/ChatInput';
import styles from './ChatPage.module.css';

export function ChatPage() {
  const { messages, loading, sending, sendMessage } = useChat();
  const bottomRef = useRef<HTMLDivElement>(null);

  useEffect(() => {
    if (messages.length > 0) {
      bottomRef.current?.scrollIntoView({ behavior: 'smooth' });
    }
  }, [messages]);

  if (loading) {
    return (
      <div className={styles.loading}>
        <span>Loading...</span>
      </div>
    );
  }

  return (
    <div className={styles.container}>
      {/* Messages */}
      <div className={styles.messageList}>
        {messages.length === 0 ? (
          <div className={styles.emptyState}>
            <p className={styles.emptyTitle}>Welcome to nexor</p>
            <p className={styles.emptySubtitle}>Start a conversation with your AI orchestrator</p>
          </div>
        ) : (
          messages.map((message) => (
            <Message key={message.id} message={message} />
          ))
        )}
        <div ref={bottomRef} />
      </div>

      {/* Input */}
      <ChatInput onSend={sendMessage} disabled={sending} />
    </div>
  );
}
```

**Create `ui/src/pages/ChatPage/ChatPage.module.css`**:
```css
.container {
  display: flex;
  flex-direction: column;
  height: 100%;
}

.loading {
  display: flex;
  align-items: center;
  justify-content: center;
  height: 16rem;
  color: var(--text-secondary);
}

.messageList {
  flex: 1;
  overflow-y: auto;
  padding-bottom: 1rem;
  display: flex;
  flex-direction: column;
  gap: 1rem;
}

.emptyState {
  text-align: center;
  color: var(--text-secondary);
  padding: 3rem 0;
}

.emptyTitle {
  font-size: 1.125rem;
  margin-bottom: 0.5rem;
}

.emptySubtitle {
  font-size: 0.875rem;
}
```

**Create `ui/src/pages/ChatPage/index.ts`**:
```ts
export { ChatPage } from './ChatPage';
```

**Verify**:
- [ ] Messages display
- [ ] Scrolls to bottom
- [ ] Empty state shows
- [ ] CSS modules apply correctly

---

### Slice 12.1.2: Create ChatInput Component

**Do this**:
- Create text input with submit button
- Support Enter to send (Shift+Enter for newline)
- Show sending state
- Use CSS modules
- Extend HTMLAttributes for textarea

**Create `ui/src/components/ChatInput/ChatInput.tsx`**:
```tsx
import { useState, KeyboardEvent, forwardRef, TextareaHTMLAttributes } from 'react';
import { Send } from 'lucide-react';
import { Button } from '../Button';
import styles from './ChatInput.module.css';

interface ChatInputProps {
  onSend: (message: string) => void;
  disabled?: boolean;
}

export const ChatInput = forwardRef<HTMLDivElement, ChatInputProps>(
  ({ onSend, disabled }, ref) => {
    const [value, setValue] = useState('');

    const handleSubmit = () => {
      if (value.trim() && !disabled) {
        onSend(value.trim());
        setValue('');
      }
    };

    const handleKeyDown = (e: KeyboardEvent<HTMLTextAreaElement>) => {
      if (e.key === 'Enter' && !e.shiftKey) {
        e.preventDefault();
        handleSubmit();
      }
    };

    return (
      <div ref={ref} className={styles.container}>
        <div className={styles.inputWrapper}>
          <textarea
            value={value}
            onChange={(e) => setValue(e.target.value)}
            onKeyDown={handleKeyDown}
            placeholder="Type a message..."
            disabled={disabled}
            rows={1}
            className={styles.textarea}
          />
          <Button
            onClick={handleSubmit}
            disabled={disabled || !value.trim()}
            variant="primary"
            size="md"
          >
            <Send size={20} />
          </Button>
        </div>
        <p className={styles.hint}>
          Press Enter to send, Shift+Enter for new line
        </p>
      </div>
    );
  }
);

ChatInput.displayName = 'ChatInput';
```

**Create `ui/src/components/ChatInput/ChatInput.module.css`**:
```css
.container {
  border-top: 1px solid var(--border);
  padding-top: 1rem;
}

.inputWrapper {
  display: flex;
  gap: 0.5rem;
}

.textarea {
  flex: 1;
  padding: 0.75rem 1rem;
  background-color: var(--bg-secondary);
  border: 1px solid var(--border);
  border-radius: 0.5rem;
  color: var(--text-primary);
  resize: none;
  transition: border-color 0.2s;
}

.textarea::placeholder {
  color: var(--text-tertiary);
}

.textarea:focus {
  outline: none;
  border-color: var(--accent);
}

.textarea:disabled {
  opacity: 0.5;
}

.hint {
  font-size: 0.75rem;
  color: var(--text-tertiary);
  margin-top: 0.5rem;
}
```

**Create `ui/src/components/ChatInput/index.ts`**:
```ts
export { ChatInput } from './ChatInput';
```

**Verify**:
- [ ] Can type messages
- [ ] Enter sends message
- [ ] Shift+Enter adds newline
- [ ] Button disabled when empty
- [ ] CSS modules apply correctly

---

### Slice 12.1.3: Create Message Component

**Do this**:
- Show streaming tokens as they arrive
- Cursor animation while streaming
- Complete message when done
- Use CSS modules for styling

**Create `ui/src/components/Message/Message.tsx`**:
```tsx
import { ChatMessage } from '../../api/client';
import { User, Bot } from 'lucide-react';
import { MarkdownContent } from '../MarkdownContent';
import styles from './Message.module.css';

interface MessageProps {
  message: ChatMessage;
  streaming?: boolean;
}

export function Message({ message, streaming }: MessageProps) {
  const isUser = message.role === 'user';

  return (
    <div className={`${styles.message} ${isUser ? styles.messageUser : styles.messageBot}`}>
      {/* Avatar */}
      <div className={`${styles.avatar} ${isUser ? styles.avatarUser : styles.avatarBot}`}>
        {isUser ? <User size={16} /> : <Bot size={16} />}
      </div>

      {/* Content */}
      <div className={styles.content}>
        <div className={styles.header}>
          <span className={styles.role}>{isUser ? 'You' : 'Orchestrator'}</span>
          <span className={styles.timestamp}>
            {new Date(message.timestamp).toLocaleTimeString()}
          </span>
        </div>
        <div className={`${styles.bubble} ${isUser ? styles.bubbleUser : styles.bubbleBot}`}>
          {isUser ? (
            <p className={styles.text}>{message.content}</p>
          ) : (
            <>
              <MarkdownContent content={message.content} />
              {streaming && <span className={styles.cursor} />}
            </>
          )}
        </div>
      </div>
    </div>
  );
}
```

**Create `ui/src/components/Message/Message.module.css`**:
```css
.message {
  display: flex;
  gap: 0.75rem;
}

.messageUser {
  flex-direction: row-reverse;
}

.messageBot {
  flex-direction: row;
}

.avatar {
  width: 2rem;
  height: 2rem;
  border-radius: 50%;
  display: flex;
  align-items: center;
  justify-content: center;
  flex-shrink: 0;
}

.avatarUser {
  background-color: var(--accent);
}

.avatarBot {
  background-color: var(--bg-tertiary);
}

.content {
  flex: 1;
  max-width: 80%;
}

.messageUser .content {
  text-align: right;
}

.header {
  font-size: 0.75rem;
  color: var(--text-tertiary);
  margin-bottom: 0.25rem;
}

.role {
  font-weight: 500;
}

.timestamp {
  margin-left: 0.5rem;
}

.bubble {
  padding: 0.75rem 1rem;
  border-radius: 0.5rem;
}

.bubbleUser {
  background-color: rgba(var(--accent-rgb), 0.2);
  color: var(--text-primary);
}

.bubbleBot {
  background-color: var(--bg-secondary);
  color: var(--text-primary);
  font-family: 'SF Mono', 'Fira Code', monospace;
  font-size: 0.875rem;
}

.text {
  white-space: pre-wrap;
}

.cursor {
  display: inline-block;
  width: 0.5rem;
  height: 1rem;
  background-color: var(--accent);
  margin-left: 0.25rem;
  animation: pulse 1s infinite;
}

@keyframes pulse {
  0%, 100% { opacity: 1; }
  50% { opacity: 0.5; }
}
```

**Create `ui/src/components/Message/index.ts`**:
```ts
export { Message } from './Message';
```

**Verify**:
- [ ] Streaming cursor visible
- [ ] Tokens appear smoothly
- [ ] Cursor disappears when done
- [ ] CSS modules apply correctly

---

### Slice 12.1.4: Add Markdown Rendering Component

**Do this**:
- Install react-markdown
- Render markdown in responses
- Support code blocks, lists, etc.
- Use component composition

**Commands**:
```bash
npm install react-markdown
npm install -D @types/react-markdown
```

**Create `ui/src/components/MarkdownContent/MarkdownContent.tsx`**:
```tsx
import ReactMarkdown from 'react-markdown';
import { CodeBlock } from '../CodeBlock';
import styles from './MarkdownContent.module.css';

interface MarkdownContentProps {
  content: string;
}

export function MarkdownContent({ content }: MarkdownContentProps) {
  return (
    <ReactMarkdown
      className={styles.markdown}
      components={{
        code({ node, inline, className, children, ...props }) {
          const match = /language-(\w+)/.exec(className || '');
          const language = match ? match[1] : '';

          if (!inline && language) {
            return (
              <CodeBlock
                code={String(children).replace(/\n$/, '')}
                language={language}
              />
            );
          }

          return (
            <code className={styles.inlineCode} {...props}>
              {children}
            </code>
          );
        },
        p({ children }) {
          return <p className={styles.paragraph}>{children}</p>;
        },
        ul({ children }) {
          return <ul className={styles.list}>{children}</ul>;
        },
        ol({ children }) {
          return <ol className={styles.orderedList}>{children}</ol>;
        },
      }}
    >
      {content}
    </ReactMarkdown>
  );
}
```

**Create `ui/src/components/MarkdownContent/MarkdownContent.module.css`**:
```css
.markdown {
  max-width: 100%;
}

.inlineCode {
  background-color: var(--bg-tertiary);
  padding: 0.125rem 0.375rem;
  border-radius: 0.25rem;
  color: var(--accent);
  font-family: 'SF Mono', 'Fira Code', monospace;
  font-size: 0.875em;
}

.paragraph {
  margin-bottom: 0.5rem;
}

.paragraph:last-child {
  margin-bottom: 0;
}

.list {
  list-style-type: disc;
  list-style-position: inside;
  margin-bottom: 0.5rem;
}

.orderedList {
  list-style-type: decimal;
  list-style-position: inside;
  margin-bottom: 0.5rem;
}
```

**Create `ui/src/components/MarkdownContent/index.ts`**:
```ts
export { MarkdownContent } from './MarkdownContent';
```

**Verify**:
- [ ] Markdown renders correctly
- [ ] Code blocks highlighted
- [ ] Lists formatted properly
- [ ] CSS modules apply correctly

---

### Slice 12.1.5: Create CodeBlock Component with Syntax Highlighting

**Do this**:
- Install Prism for syntax highlighting
- Create CodeBlock component with copy functionality
- Support common languages
- Use CSS modules

**Commands**:
```bash
npm install prism-react-renderer
```

**Create `ui/src/components/CodeBlock/CodeBlock.tsx`**:
```tsx
import { Highlight, themes } from 'prism-react-renderer';
import { Copy, Check } from 'lucide-react';
import { useState, useEffect } from 'react';
import styles from './CodeBlock.module.css';

interface CodeBlockProps {
  code: string;
  language: string;
}

export function CodeBlock({ code, language }: CodeBlockProps) {
  const [copied, setCopied] = useState(false);

  const handleCopy = () => {
    navigator.clipboard.writeText(code);
    setCopied(true);
  };

  useEffect(() => {
    if (copied) {
      const timer = setTimeout(() => setCopied(false), 2000);
      return () => clearTimeout(timer);
    }
  }, [copied]);

  return (
    <div className={styles.container}>
      <div className={styles.header}>
        <span className={styles.language}>{language}</span>
        <button
          onClick={handleCopy}
          className={styles.copyButton}
          aria-label="Copy code"
        >
          {copied ? <Check size={14} /> : <Copy size={14} />}
        </button>
      </div>
      <Highlight theme={themes.nightOwl} code={code} language={language}>
        {({ style, tokens, getLineProps, getTokenProps }) => (
          <pre className={styles.pre} style={style}>
            {tokens.map((line, i) => (
              <div key={i} {...getLineProps({ line })} className={styles.line}>
                <span className={styles.lineNumber}>
                  {String(i + 1).padStart(2, ' ')}
                </span>
                {line.map((token, key) => (
                  <span key={key} {...getTokenProps({ token })} />
                ))}
              </div>
            ))}
          </pre>
        )}
      </Highlight>
    </div>
  );
}
```

**Create `ui/src/components/CodeBlock/CodeBlock.module.css`**:
```css
.container {
  position: relative;
  margin: 0.5rem 0;
  border-radius: 0.5rem;
  overflow: hidden;
}

.header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: 0.5rem 0.75rem;
  background-color: var(--bg-tertiary);
  border-bottom: 1px solid var(--border);
}

.language {
  font-size: 0.75rem;
  color: var(--text-tertiary);
  font-family: 'SF Mono', 'Fira Code', monospace;
}

.copyButton {
  padding: 0.375rem;
  background-color: transparent;
  border: none;
  color: var(--text-secondary);
  cursor: pointer;
  border-radius: 0.25rem;
  transition: all 0.2s;
  display: flex;
  align-items: center;
}

.copyButton:hover {
  background-color: var(--border);
  color: var(--text-primary);
}

.pre {
  padding: 0.75rem;
  background-color: var(--bg-primary);
  overflow-x: auto;
  font-size: 0.875rem;
  margin: 0;
}

.line {
  display: flex;
}

.lineNumber {
  color: var(--text-tertiary);
  margin-right: 1rem;
  user-select: none;
  min-width: 1.5rem;
  text-align: right;
}
```

**Create `ui/src/components/CodeBlock/index.ts`**:
```ts
export { CodeBlock } from './CodeBlock';
```

**Verify**:
- [ ] Code highlighted correctly
- [ ] Line numbers visible
- [ ] Copy button works
- [ ] Language label shows
- [ ] Timer cleanup works
- [ ] CSS modules apply correctly

---

## Completion Checklist

Before marking this ticket done:

- [ ] All slices verified
- [ ] Messages display correctly
- [ ] Streaming works
- [ ] Markdown renders
- [ ] Code highlighted
- [ ] CSS modules used throughout
- [ ] useEffect cleanup implemented
- [ ] Component composition patterns followed
- [ ] `PROGRESS.md` updated with 12.1 status
