# Ticket 12.5: File Browser & Editor

> Browse and edit project files with syntax highlighting.

## Goal

File tree browser and code editor for viewing/editing project files.

**Checkpoint**: Can browse files, view with syntax highlighting, edit and save.

---

## Slices

### Slice 12.5.1: Add File API Endpoints to Rust

Add endpoints to `src/server/api.rs`:
- `GET /api/files` - List files/directories
- `GET /api/files/*path` - Read file content
- `PUT /api/files/*path` - Write file content

---

### Slice 12.5.2: Create FileBrowser Component

**Create `ui/src/components/FileBrowser.tsx`**:
```tsx
import { useState, useEffect } from 'react';
import { ChevronRight, ChevronDown, File, Folder } from 'lucide-react';

interface FileNode {
  name: string;
  path: string;
  type: 'file' | 'directory';
  children?: FileNode[];
}

interface FileBrowserProps {
  onSelect: (path: string) => void;
}

export function FileBrowser({ onSelect }: FileBrowserProps) {
  const [tree, setTree] = useState<FileNode[]>([]);
  const [expanded, setExpanded] = useState<Set<string>>(new Set());

  useEffect(() => {
    // Fetch file tree
    fetch('/api/files')
      .then(r => r.json())
      .then(setTree);
  }, []);

  const toggleExpand = (path: string) => {
    setExpanded(prev => {
      const next = new Set(prev);
      if (next.has(path)) next.delete(path);
      else next.add(path);
      return next;
    });
  };

  const renderNode = (node: FileNode, depth = 0) => (
    <div key={node.path}>
      <div
        className={`flex items-center gap-1 py-1 px-2 hover:bg-bg-tertiary cursor-pointer
                    text-sm ${node.type === 'file' ? 'text-text-primary' : 'text-text-secondary'}`}
        style={{ paddingLeft: `${depth * 16 + 8}px` }}
        onClick={() => node.type === 'file' ? onSelect(node.path) : toggleExpand(node.path)}
      >
        {node.type === 'directory' ? (
          expanded.has(node.path) ? <ChevronDown size={14} /> : <ChevronRight size={14} />
        ) : null}
        {node.type === 'directory' ? <Folder size={14} /> : <File size={14} />}
        <span className="truncate">{node.name}</span>
      </div>
      {node.type === 'directory' && expanded.has(node.path) && node.children?.map(child =>
        renderNode(child, depth + 1)
      )}
    </div>
  );

  return (
    <div className="h-full overflow-auto bg-bg-secondary border-r border-border">
      {tree.map(node => renderNode(node))}
    </div>
  );
}
```

---

### Slice 12.5.3: Create FileViewer with Syntax Highlighting

Use the same syntax highlighting from chat for viewing files.

---

### Slice 12.5.4: Create FileEditor with Monaco/CodeMirror

**Commands**:
```bash
npm install @monaco-editor/react
```

**Create `ui/src/components/FileEditor.tsx`**:
```tsx
import Editor from '@monaco-editor/react';
import { useState } from 'react';
import { Save, X } from 'lucide-react';

interface FileEditorProps {
  path: string;
  content: string;
  language: string;
  onSave: (content: string) => Promise<void>;
  onClose: () => void;
}

export function FileEditor({ path, content, language, onSave, onClose }: FileEditorProps) {
  const [value, setValue] = useState(content);
  const [saving, setSaving] = useState(false);
  const modified = value !== content;

  const handleSave = async () => {
    setSaving(true);
    try {
      await onSave(value);
    } finally {
      setSaving(false);
    }
  };

  return (
    <div className="h-full flex flex-col">
      <div className="flex items-center justify-between p-2 bg-bg-secondary border-b border-border">
        <div className="flex items-center gap-2">
          <span className="text-sm text-text-primary">{path}</span>
          {modified && <span className="text-xs text-accent">Modified</span>}
        </div>
        <div className="flex items-center gap-2">
          <button
            onClick={handleSave}
            disabled={!modified || saving}
            className="flex items-center gap-1 px-3 py-1 bg-accent text-white rounded text-sm
                       disabled:opacity-50"
          >
            <Save size={14} />
            Save
          </button>
          <button onClick={onClose} className="p-1 text-text-secondary hover:text-text-primary">
            <X size={16} />
          </button>
        </div>
      </div>
      <div className="flex-1">
        <Editor
          height="100%"
          language={language}
          value={value}
          onChange={(v) => setValue(v || '')}
          theme="vs-dark"
          options={{
            minimap: { enabled: false },
            fontSize: 14,
            fontFamily: 'SF Mono, Fira Code, monospace',
            lineNumbers: 'on',
            scrollBeyondLastLine: false,
          }}
        />
      </div>
    </div>
  );
}
```

---

### Slice 12.5.5: Implement Save with Confirmation

Add save confirmation dialog and API call.

---

## Completion Checklist

- [ ] File tree displays
- [ ] Can navigate directories
- [ ] Files viewable with highlighting
- [ ] Editor works
- [ ] Save works
