# Ticket 12.2: Feed View

> Real-time agent activity feed.

## Goal

Live feed showing what agents are doing in natural language.

**Checkpoint**: Feed updates in real-time, shows agent activity, auto-scrolls.

---

## Context

The feed provides visibility into agent work - like watching a terminal log.

**Dependencies**:
- M11 (React foundation)
- M10.4 (WebSocket)

---

## Slices

### Slice 12.2.1: Create FeedPage with Scrollable Feed

**Do this**:
- Create FeedPage component
- Display feed items in reverse chronological order
- Auto-scroll to newest items
- Use CSS modules for styling
- Add proper useEffect cleanup

**Create `ui/src/pages/FeedPage/FeedPage.tsx`**:
```tsx
import { useEffect, useRef } from 'react';
import { useFeed } from '../../hooks/useFeed';
import { FeedItem } from '../../components/FeedItem';
import styles from './FeedPage.module.css';

export function FeedPage() {
  const { items } = useFeed();
  const bottomRef = useRef<HTMLDivElement>(null);

  useEffect(() => {
    if (items.length > 0) {
      bottomRef.current?.scrollIntoView({ behavior: 'smooth' });
    }
  }, [items]);

  return (
    <div className={styles.container}>
      <div className={styles.header}>
        <h2 className={styles.title}>Agent Activity</h2>
        <span className={styles.count}>
          {items.length} events
        </span>
      </div>

      {items.length === 0 ? (
        <div className={styles.emptyState}>
          <p className={styles.emptyTitle}>No agent activity yet</p>
          <p className={styles.emptySubtitle}>Start a task to see agents working</p>
        </div>
      ) : (
        <div className={styles.feedList}>
          {items.map((item) => (
            <FeedItem key={item.id} item={item} />
          ))}
          <div ref={bottomRef} />
        </div>
      )}
    </div>
  );
}
```

**Create `ui/src/pages/FeedPage/FeedPage.module.css`**:
```css
.container {
  display: flex;
  flex-direction: column;
  height: 100%;
  gap: 1rem;
}

.header {
  display: flex;
  align-items: center;
  justify-content: space-between;
  margin-bottom: 1rem;
}

.title {
  font-size: 1.125rem;
  font-weight: 500;
  color: var(--text-primary);
}

.count {
  font-size: 0.875rem;
  color: var(--text-secondary);
}

.emptyState {
  text-align: center;
  color: var(--text-secondary);
  padding: 3rem 0;
}

.emptyTitle {
  font-size: 1rem;
  margin-bottom: 0.5rem;
}

.emptySubtitle {
  font-size: 0.875rem;
}

.feedList {
  display: flex;
  flex-direction: column;
  gap: 0.25rem;
  overflow-y: auto;
}
```

**Create `ui/src/pages/FeedPage/index.ts`**:
```ts
export { FeedPage } from './FeedPage';
```

**Verify**:
- [ ] Feed displays items
- [ ] Auto-scrolls to bottom
- [ ] Empty state shows
- [ ] CSS modules apply correctly

---

### Slice 12.2.2: Create FeedItem Component

**Do this**:
- Create FeedItem component
- Style differently based on type
- Show timestamp and agent name
- Use CSS modules for styling

**Create `ui/src/components/FeedItem/FeedItem.tsx`**:
```tsx
import { FeedItem as FeedItemType } from '../../hooks/useFeed';
import { StatusDot } from '../StatusDot';
import styles from './FeedItem.module.css';

interface FeedItemProps {
  item: FeedItemType;
}

export function FeedItem({ item }: FeedItemProps) {
  const getAgentStyle = (agent: string): string => {
    if (agent.toLowerCase().includes('orchestrator')) return styles.agentOrchestrator;
    if (agent.toLowerCase().includes('worker')) return styles.agentWorker;
    return styles.agentDefault;
  };

  const getTypeStyle = (type: string): string => {
    switch (type) {
      case 'milestone':
        return styles.typeMilestone;
      case 'error':
        return styles.typeError;
      case 'warning':
        return styles.typeWarning;
      default:
        return styles.typeDefault;
    }
  };

  const getStatusType = (type: string): 'success' | 'error' | 'active' | 'idle' => {
    if (type === 'error') return 'error';
    if (type === 'milestone') return 'success';
    return 'active';
  };

  return (
    <div className={`${styles.item} ${getTypeStyle(item.type)}`}>
      {/* Timestamp */}
      <span className={styles.timestamp}>
        {new Date(item.timestamp).toLocaleTimeString()}
      </span>

      {/* Status dot */}
      <StatusDot status={getStatusType(item.type)} />

      {/* Agent name */}
      <span className={`${styles.agent} ${getAgentStyle(item.agent)}`}>
        {item.agent}
      </span>

      {/* Content */}
      <span className={styles.content}>
        {item.content}
      </span>
    </div>
  );
}
```

**Create `ui/src/components/FeedItem/FeedItem.module.css`**:
```css
.item {
  display: flex;
  gap: 0.75rem;
  padding: 0.5rem 0.75rem;
  border-radius: 0 0.375rem 0.375rem 0;
  border-left: 2px solid var(--border);
  font-family: 'SF Mono', 'Fira Code', monospace;
  font-size: 0.875rem;
  align-items: center;
}

.typeDefault {
  border-left-color: var(--border);
}

.typeMilestone {
  border-left-color: var(--status-success);
  background-color: rgba(var(--status-success-rgb), 0.05);
}

.typeError {
  border-left-color: var(--status-error);
  background-color: rgba(var(--status-error-rgb), 0.05);
}

.typeWarning {
  border-left-color: var(--status-warning);
  background-color: rgba(var(--status-warning-rgb), 0.05);
}

.timestamp {
  color: var(--text-tertiary);
  flex-shrink: 0;
  min-width: 5rem;
}

.agent {
  font-weight: 500;
  flex-shrink: 0;
  min-width: 8rem;
}

.agentOrchestrator {
  color: var(--accent);
}

.agentWorker {
  color: var(--accent-secondary);
}

.agentDefault {
  color: var(--text-secondary);
}

.content {
  color: var(--text-primary);
  flex: 1;
}
```

**Create `ui/src/components/FeedItem/index.ts`**:
```ts
export { FeedItem } from './FeedItem';
```

**Verify**:
- [ ] Items styled by type
- [ ] Agent name colored
- [ ] Timestamp visible
- [ ] CSS modules apply correctly

---

### Slice 12.2.3: Subscribe to WebSocket Feed Channel

**Do this**:
- Connect to WebSocket on mount
- Subscribe to "feed" channel
- Update items on new events
- **Add proper cleanup for WebSocket subscriptions**

**Update `ui/src/hooks/useFeed.ts`**:
```tsx
import { useState, useEffect, useCallback } from 'react';
import { wsClient } from '../api/websocket';

export interface FeedItem {
  id: string;
  agent: string;
  content: string;
  type: 'report' | 'milestone' | 'error' | 'warning' | 'system';
  timestamp: string;
}

export function useFeed() {
  const [items, setItems] = useState<FeedItem[]>([]);
  const [connected, setConnected] = useState(false);

  useEffect(() => {
    let mounted = true;

    // Connect WebSocket
    wsClient.connect().then(() => {
      if (mounted) {
        setConnected(true);
        wsClient.subscribe(['feed']);
      }
    });

    // Handle feed events
    const handleFeed = (data: FeedItem) => {
      if (mounted) {
        setItems((prev) => [...prev, data].slice(-200)); // Keep last 200
      }
    };

    wsClient.on('feed', handleFeed);

    return () => {
      mounted = false;
      wsClient.off('feed', handleFeed);
      wsClient.unsubscribe(['feed']);
    };
  }, []);

  const clear = useCallback(() => {
    setItems([]);
  }, []);

  return { items, connected, clear };
}
```

**Verify**:
- [ ] WebSocket connects
- [ ] Feed items appear in real-time
- [ ] Cleanup on unmount works
- [ ] No memory leaks

---

### Slice 12.2.4: Add Auto-scroll with "New Messages" Indicator

**Do this**:
- Detect when user scrolls up
- Show "new messages" indicator
- Click to scroll to bottom
- Use CSS modules
- Add proper cleanup

**Update `ui/src/pages/FeedPage/FeedPage.tsx`**:
```tsx
import { useEffect, useRef, useState } from 'react';
import { useFeed } from '../../hooks/useFeed';
import { FeedItem } from '../../components/FeedItem';
import { ArrowDown } from 'lucide-react';
import styles from './FeedPage.module.css';

export function FeedPage() {
  const { items } = useFeed();
  const containerRef = useRef<HTMLDivElement>(null);
  const bottomRef = useRef<HTMLDivElement>(null);
  const [autoScroll, setAutoScroll] = useState(true);
  const [newCount, setNewCount] = useState(0);

  // Check if user has scrolled up
  const handleScroll = () => {
    if (!containerRef.current) return;
    const { scrollTop, scrollHeight, clientHeight } = containerRef.current;
    const isAtBottom = scrollHeight - scrollTop - clientHeight < 50;
    setAutoScroll(isAtBottom);
    if (isAtBottom) setNewCount(0);
  };

  // Auto-scroll on new items
  useEffect(() => {
    if (autoScroll) {
      bottomRef.current?.scrollIntoView({ behavior: 'smooth' });
    } else {
      setNewCount((prev) => prev + 1);
    }
  }, [items, autoScroll]);

  const scrollToBottom = () => {
    bottomRef.current?.scrollIntoView({ behavior: 'smooth' });
    setAutoScroll(true);
    setNewCount(0);
  };

  return (
    <div className={styles.container}>
      <div className={styles.header}>
        <h2 className={styles.title}>Agent Activity</h2>
        <span className={styles.count}>
          {items.length} events
        </span>
      </div>

      <div className={styles.contentWrapper}>
        {items.length === 0 ? (
          <div className={styles.emptyState}>
            <p className={styles.emptyTitle}>No agent activity yet</p>
            <p className={styles.emptySubtitle}>Start a task to see agents working</p>
          </div>
        ) : (
          <div
            ref={containerRef}
            onScroll={handleScroll}
            className={styles.feedList}
          >
            {items.map((item) => (
              <FeedItem key={item.id} item={item} />
            ))}
            <div ref={bottomRef} />
          </div>
        )}

        {/* New messages indicator */}
        {!autoScroll && newCount > 0 && (
          <button
            onClick={scrollToBottom}
            className={styles.newEventsButton}
          >
            <ArrowDown size={16} />
            {newCount} new {newCount === 1 ? 'event' : 'events'}
          </button>
        )}
      </div>
    </div>
  );
}
```

**Update `ui/src/pages/FeedPage/FeedPage.module.css`**:
```css
.container {
  display: flex;
  flex-direction: column;
  height: 100%;
}

.header {
  display: flex;
  align-items: center;
  justify-content: space-between;
  margin-bottom: 1rem;
  flex-shrink: 0;
}

.title {
  font-size: 1.125rem;
  font-weight: 500;
  color: var(--text-primary);
}

.count {
  font-size: 0.875rem;
  color: var(--text-secondary);
}

.contentWrapper {
  position: relative;
  flex: 1;
  overflow: hidden;
}

.emptyState {
  text-align: center;
  color: var(--text-secondary);
  padding: 3rem 0;
}

.emptyTitle {
  font-size: 1rem;
  margin-bottom: 0.5rem;
}

.emptySubtitle {
  font-size: 0.875rem;
}

.feedList {
  height: 100%;
  overflow-y: auto;
  display: flex;
  flex-direction: column;
  gap: 0.25rem;
}

.newEventsButton {
  position: absolute;
  bottom: 1rem;
  left: 50%;
  transform: translateX(-50%);
  display: flex;
  align-items: center;
  gap: 0.5rem;
  padding: 0.5rem 1rem;
  background-color: var(--accent);
  color: white;
  border: none;
  border-radius: 9999px;
  box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
  cursor: pointer;
  font-size: 0.875rem;
  font-weight: 500;
  transition: all 0.2s;
}

.newEventsButton:hover {
  background-color: var(--accent-hover);
  box-shadow: 0 6px 8px rgba(0, 0, 0, 0.15);
}
```

**Verify**:
- [ ] Auto-scroll when at bottom
- [ ] Pauses when user scrolls up
- [ ] "New messages" indicator shows
- [ ] Click scrolls to bottom
- [ ] CSS modules apply correctly

---

## Completion Checklist

Before marking this ticket done:

- [ ] All slices verified
- [ ] Feed displays items
- [ ] Real-time updates work
- [ ] Auto-scroll behavior correct
- [ ] CSS modules used throughout
- [ ] useEffect cleanup implemented
- [ ] WebSocket cleanup prevents memory leaks
- [ ] `PROGRESS.md` updated with 12.2 status
