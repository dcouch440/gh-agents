# Ticket 12.6: Polish & Integration

> Markdown rendering, Makefile targets, and error handling polish.

## Goal

Terminal markdown rendering for assistant responses, Makefile integration, and robust error handling for a production-quality CLI experience.

**Checkpoint**: Markdown renders beautifully in terminal, `make cli-dev` works, errors are handled gracefully.

---

## Context

Assistant responses contain markdown (headers, code blocks, lists, bold/italic). We need to render this in the terminal, not as raw text.

**Key files**:
- `cli/src/components/Message.tsx` - Message rendering (from 12.4)
- `cli/src/components/StreamingMessage.tsx` - Streaming message (from 12.5)
- `Makefile` - Build targets

**Dependencies**:
- 12.5 (streaming works)

---

## Slices

### Slice 12.6.1: Terminal Markdown Rendering

**Do this**:
- Use `marked` + `marked-terminal` to convert markdown to terminal-formatted text
- Apply to assistant message content in both Message and StreamingMessage
- Headers become bold + colored
- Code blocks get background/border treatment
- Inline code gets highlighted
- Lists render with bullet characters

**Create `cli/src/utils/markdown.ts`**:
```typescript
import { marked } from 'marked';
import TerminalRenderer from 'marked-terminal';

// Configure marked with terminal renderer
// Export: renderMarkdown(content: string) → string
```

**Modify `cli/src/components/Message.tsx`**:
- For assistant messages, pass content through `renderMarkdown()` before displaying

**Modify `cli/src/components/StreamingMessage.tsx`**:
- Render partial content through `renderMarkdown()` as tokens accumulate

**Verify**:
- [ ] Headers render as bold colored text
- [ ] Code blocks have visual distinction
- [ ] Lists render with bullets
- [ ] Inline code is highlighted
- [ ] Streaming markdown doesn't break mid-render

---

### Slice 12.6.2: Error Handling & Edge Cases

**Do this**:
- Handle backend not running (connection refused → helpful error message)
- Handle network errors during streaming (reconnect or show error)
- Handle Ctrl+C cleanly (cleanup SSE connections, exit gracefully)
- Handle terminal resize (Ink handles this, but verify)
- Add `--version` flag that prints version from package.json

**Modify `cli/src/App.tsx`**:
- Add error boundary or try/catch around API calls
- Show connection error state with retry option

**Modify `cli/src/index.tsx`**:
- Handle `--version` and `--help` flags before rendering Ink app
- Set up process signal handlers for clean exit

**Verify**:
- [ ] Stop backend → run CLI → see "Cannot connect to server" message
- [ ] `npm run dev -- --version` prints version
- [ ] Ctrl+C exits cleanly without error output
- [ ] Network error during streaming shows error and recovers

---

### Slice 12.6.3: Makefile Integration

**Do this**:
- Add CLI targets to the root Makefile
- Update help text

**Modify `Makefile`**:
```makefile
# --- CLI (Terminal) ---
cli-install:
	cd cli && npm install

cli-dev:
	cd cli && npm run dev

cli-build:
	cd cli && npm run build
```

Update the `dev` target to offer a choice or document how to run CLI vs web UI.

Update `help` target to include CLI section.

Update `build-all` and `ci` to include `cli-build`.

Update `clean` to include `rm -rf cli/dist cli/node_modules/.cache`.

**Verify**:
- [ ] `make cli-install` installs dependencies
- [ ] `make cli-dev` launches the CLI
- [ ] `make cli-build` compiles TypeScript
- [ ] `make help` shows CLI targets
- [ ] `make build-all` includes CLI build

---

## Completion Checklist

Before marking this ticket done:

- [ ] All slices verified
- [ ] Markdown renders correctly in terminal
- [ ] Error states handled gracefully
- [ ] Makefile targets work
- [ ] Clean process exit
- [ ] `npx tsc --noEmit` passes
- [ ] Code follows `CONVENTIONS.md`
- [ ] `PROGRESS.md` updated
