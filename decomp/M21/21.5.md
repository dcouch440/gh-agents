# Ticket 21.5: Integration Tests

> End-to-end tests for the complete branch switching flow.

## Goal

Integration tests verify both branch switch paths work correctly from trigger through to visual output.

**Checkpoint**: All integration tests pass.

---

## Context

These tests mock both the WebSocket layer and the git filesystem to test the full flow.

**Key files**:
- `cli/src/components/MainView.test.tsx` — Extend
- `cli/src/hooks/useGitBranch.test.ts` — Extend if needed

**Dependencies**: 21.4

---

## Slices

### Slice 21.5.1: Explicit Branch Switch Flow

**Do this**:
- Test: type `/branch` → selector appears → select `feature/xyz` → divider appears in feed → StatusBar shows `feature/xyz` → selector closes
- Test: type `/branch main` → divider appears without selector opening
- Test: type `/branch` then Esc → selector closes, no switch

**Verify**:
- [ ] Tests pass

---

### Slice 21.5.2: Auto-Detection Flow

**Do this**:
- Test: mock fs.watch fires `.git/HEAD` change → branch read returns `develop` → divider appears in feed → StatusBar updates
- Test: rapid git checkout (multiple changes within 100ms) → only one divider appears (debounce)
- Test: switching to same branch externally → "Already on branch" message, no divider

**Verify**:
- [ ] Tests pass

---

### Slice 21.5.3: Stale Item Visual Flow

**Do this**:
- Test: push 3 feed items → switch branch → first 3 items are dimmed, divider present, new items below are not dimmed
- Test: switch branch twice → items between dividers are dimmed after second switch
- Test: `/clear` after branch switch → all items cleared including dividers

**Verify**:
- [ ] Tests pass
- [ ] `cd cli && npm test` all green
- [ ] `PROGRESS.md` updated

---

## Completion Checklist

- [ ] All slices verified
- [ ] Explicit switch flow tested
- [ ] Auto-detection flow tested
- [ ] Stale rendering tested
- [ ] `PROGRESS.md` updated
