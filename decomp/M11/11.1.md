# Ticket 11.1: Project Setup

> Initialize React project with Vite, TypeScript, and TailwindCSS.

## Goal

A working React development environment with all tooling configured.

**Checkpoint**: `npm run dev` starts Vite, page loads in browser with styled content.

---

## Context

This is the first React ticket. We're creating the frontend project from scratch.

**Key files**:
- `doc/DESIGN-SYSTEM.md` - Design tokens to configure in Tailwind

**Dependencies**:
- M10.1 (server running for API proxy)

---

## Slices

### Slice 11.1.1: Create Vite + React + TypeScript Project

**Do this**:
- Create `ui/` directory in project root
- Initialize Vite with React TypeScript template
- Verify dev server starts

**Commands**:
```bash
cd /path/to/nexor
npm create vite@latest ui -- --template react-ts
cd ui
npm install
npm run dev
```

**Verify**:
- [ ] `ui/` directory created
- [ ] `npm run dev` starts on port 5173
- [ ] Default Vite page loads in browser

---

### Slice 11.1.2: Add TailwindCSS with Design Tokens

**Do this**:
- Install TailwindCSS and dependencies
- Configure with Claude Code-inspired colors
- Add base styles

**Commands**:
```bash
cd ui
npm install -D tailwindcss postcss autoprefixer
npx tailwindcss init -p
```

**Create `ui/tailwind.config.js`**:
```js
/** @type {import('tailwindcss').Config} */
export default {
  content: [
    "./index.html",
    "./src/**/*.{js,ts,jsx,tsx}",
  ],
  darkMode: 'class',
  theme: {
    extend: {
      colors: {
        bg: {
          primary: '#0d1117',
          secondary: '#161b22',
          tertiary: '#21262d',
        },
        text: {
          primary: '#e6edf3',
          secondary: '#8b949e',
          tertiary: '#6e7681',
        },
        accent: {
          DEFAULT: '#da7756',
          secondary: '#58a6ff',
        },
        border: {
          DEFAULT: '#30363d',
          muted: '#21262d',
        },
        status: {
          success: '#3fb950',
          warning: '#d29922',
          error: '#f85149',
          info: '#58a6ff',
        },
      },
      fontFamily: {
        sans: ['-apple-system', 'BlinkMacSystemFont', 'Segoe UI', 'Noto Sans', 'Helvetica', 'Arial', 'sans-serif'],
        mono: ['SF Mono', 'Fira Code', 'Fira Mono', 'Menlo', 'Consolas', 'monospace'],
      },
    },
  },
  plugins: [],
}
```

**Update `ui/src/index.css`**:
```css
@tailwind base;
@tailwind components;
@tailwind utilities;

:root {
  color-scheme: dark;
}

body {
  @apply bg-bg-primary text-text-primary font-sans;
}

/* Scrollbar styling */
::-webkit-scrollbar {
  width: 8px;
  height: 8px;
}

::-webkit-scrollbar-track {
  @apply bg-bg-secondary;
}

::-webkit-scrollbar-thumb {
  @apply bg-border rounded-full;
}

::-webkit-scrollbar-thumb:hover {
  @apply bg-text-tertiary;
}
```

**Verify**:
- [ ] TailwindCSS classes work
- [ ] Dark theme colors apply
- [ ] Custom colors accessible

---

### Slice 11.1.3: Add React Router for Navigation

**Do this**:
- Install React Router
- Set up basic route structure
- Create placeholder pages

**Commands**:
```bash
npm install react-router-dom
```

**Update `ui/src/App.tsx`**:
```tsx
import { BrowserRouter, Routes, Route, Navigate } from 'react-router-dom';

// Placeholder pages (will be replaced)
const LoginPage = () => <div className="p-8">Login</div>;
const DashboardPage = () => <div className="p-8">Dashboard</div>;

function App() {
  return (
    <BrowserRouter>
      <Routes>
        <Route path="/login" element={<LoginPage />} />
        <Route path="/" element={<DashboardPage />} />
        <Route path="*" element={<Navigate to="/" replace />} />
      </Routes>
    </BrowserRouter>
  );
}

export default App;
```

**Verify**:
- [ ] `/login` shows Login page
- [ ] `/` shows Dashboard page
- [ ] Unknown routes redirect to `/`

---

### Slice 11.1.4: Add Zustand for State Management

**Do this**:
- Install Zustand
- Create base store with auth state
- Persist auth token to localStorage

**Commands**:
```bash
npm install zustand
```

**Create `ui/src/store/index.ts`**:
```ts
import { create } from 'zustand';
import { persist } from 'zustand/middleware';

interface AuthState {
  token: string | null;
  isAuthenticated: boolean;
  setToken: (token: string | null) => void;
  logout: () => void;
}

export const useAuthStore = create<AuthState>()(
  persist(
    (set) => ({
      token: null,
      isAuthenticated: false,
      setToken: (token) => set({ token, isAuthenticated: !!token }),
      logout: () => set({ token: null, isAuthenticated: false }),
    }),
    {
      name: 'nexor-auth',
    }
  )
);

interface AppState {
  sidebarCollapsed: boolean;
  toggleSidebar: () => void;
}

export const useAppStore = create<AppState>((set) => ({
  sidebarCollapsed: false,
  toggleSidebar: () => set((state) => ({ sidebarCollapsed: !state.sidebarCollapsed })),
}));
```

**Verify**:
- [ ] Store imports without errors
- [ ] Token persists to localStorage
- [ ] State updates trigger re-renders

---

### Slice 11.1.5: Configure Vite Proxy to Rust Backend

**Do this**:
- Configure Vite to proxy /api requests to Rust server
- Add environment variables support
- Create .env.example

**Update `ui/vite.config.ts`**:
```ts
import { defineConfig } from 'vite';
import react from '@vitejs/plugin-react';

export default defineConfig({
  plugins: [react()],
  server: {
    port: 5173,
    proxy: {
      '/api': {
        target: 'http://localhost:3000',
        changeOrigin: true,
      },
      '/ws': {
        target: 'ws://localhost:3000',
        ws: true,
      },
    },
  },
});
```

**Create `ui/.env.example`**:
```
VITE_API_URL=http://localhost:3000
```

**Verify**:
- [ ] API requests proxy to Rust server
- [ ] WebSocket connections proxy correctly
- [ ] No CORS errors in development

---

## Completion Checklist

Before marking this ticket done:

- [ ] All slices verified
- [ ] `npm run dev` works
- [ ] TailwindCSS configured
- [ ] React Router routing works
- [ ] Zustand store functional
- [ ] Proxy configuration works
- [ ] `PROGRESS.md` updated with 11.1 status
