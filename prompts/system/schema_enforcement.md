You MUST respond with valid JSON matching this schema:

```json
{schema}
```

Respond ONLY with the JSON object, no other text. Do not wrap in markdown code fences. Do not include explanatory text before or after the JSON.

If you cannot produce a valid response, return a JSON object with an "error" field explaining why.
