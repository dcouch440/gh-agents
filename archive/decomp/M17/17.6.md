# Ticket 17.6: Consumer File Updates

> Update all 10 files outside `src/db/` that import `SqlitePool` to use `PgPool`.

## Goal

Every file that references `SqlitePool` is updated to `PgPool`. The entire codebase compiles.

**Checkpoint**: `cargo check` passes with zero errors.

---

## Context

~10 files across server, orchestration, LLM, observability, and GitHub modules accept `SqlitePool` as a parameter. Each needs a simple type swap, plus any pool-specific configuration changes.

**Key files** (blast radius):
- `src/server/api.rs`
- `src/server/ws.rs`
- `src/orchestration/planner.rs`
- `src/orchestration/queue.rs`
- `src/orchestration/scheduler.rs`
- `src/llm/cost.rs`
- `src/observability/export.rs`
- `src/observability/logging.rs`
- `src/github/comments.rs`
- `src/headless.rs`

**Dependencies**: 17.4 and 17.5 (query modules must compile first)

---

## Slices

### Slice 17.6.1: Server Module (api.rs, ws.rs)

**Do this**:
- Replace `SqlitePool` with `PgPool` in function signatures, struct fields, and `State` extractors
- Update any `Extension<SqlitePool>` to `Extension<PgPool>`
- Update AppState struct if it holds the pool

**Verify**:
- [ ] `src/server/api.rs` compiles
- [ ] `src/server/ws.rs` compiles

---

### Slice 17.6.2: Orchestration Module (planner.rs, queue.rs, scheduler.rs)

**Do this**:
- Replace `SqlitePool` with `PgPool` in all three files
- Update struct fields and function parameters

**Verify**:
- [ ] All three orchestration files compile

---

### Slice 17.6.3: LLM & Observability (cost.rs, export.rs, logging.rs)

**Do this**:
- Replace `SqlitePool` with `PgPool` in cost tracking and observability modules
- Update any database write calls

**Verify**:
- [ ] `src/llm/cost.rs` compiles
- [ ] `src/observability/export.rs` compiles
- [ ] `src/observability/logging.rs` compiles

---

### Slice 17.6.4: GitHub & Headless (comments.rs, headless.rs)

**Do this**:
- Replace `SqlitePool` with `PgPool` in GitHub comments and headless mode
- Update `main.rs` if it passes the pool to these modules
- Update `src/main.rs` pool initialization to use `PgPool`

**Verify**:
- [ ] `src/github/comments.rs` compiles
- [ ] `src/headless.rs` compiles
- [ ] `cargo check` passes for the entire project (zero errors)
- [ ] `cargo clippy` clean

---

## Completion Checklist

Before marking this ticket done:

- [ ] All slices verified
- [ ] Zero references to `SqlitePool` remain in the codebase
- [ ] `cargo check` passes
- [ ] `cargo clippy` passes
- [ ] `PROGRESS.md` updated
